datasource db {
  provider = "sqlite"
  url      = "file:../data/data.db"
}

generator kysely {
  provider = "prisma-kysely"

  output   = "../src/lib/db"
  fileName = "schema.d.ts"
}

model Sheet {
  id   String @id
  name String

  created_at DateTime
  updated_at DateTime

  data Bytes
}
